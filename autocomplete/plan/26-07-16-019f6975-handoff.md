# `/agent` eager autocomplete handoff — 2026-07-16

## Outcome

Work stopped at the user's request after the finite-domain/path follow-up was implemented and independently exercised by automated tests. The tree is intentionally uncommitted. Preserve unrelated `agent/settings.json`, the plan history, the blocked singular `--theme <path>` policy, and supported `--no-themes` behavior.

There is no active team. Both recovery teams disappeared before delivering a final completion report; do not infer evidence that was never recorded.

## Last reliable implementation state

The shared `command-line.ts` declaration/scanner still drives runtime parsing and editor completion. The follow-up behavior present in the tree is:

- `--tools` and `--exclude-tools` use the live `getAllTools()` catalog. Accepting `gr` as `grep` leaves `grep│` with no inserted space or comma. Typing `,` opens the next segment and excludes already-selected tools.
- Manually typing a path option boundary eagerly opens native filesystem completion for `-e`/`--extension`, `--skill`, and `--prompt-template`. Tests use valid fixtures and retain recursive fallback and cursor behavior.
- Installed skill and prompt-template shortcuts come from exact public `pi.getCommands()` sources and insert `sourceInfo.path`, including quoted paths with spaces. Arbitrary filesystem fallback remains available. Extension shortcuts are excluded because discovery is incomplete.
- No theme catalog exists. `--theme <path>` remains blocked and value-taking for accurate prose advisories; `--no-themes` remains supported.

[`README.md`](../../README.md), [`PARSER_SPEC.md`](../../PARSER_SPEC.md), and [`AUTOCOMPLETE_PLAN.md`](AUTOCOMPLETE_PLAN.md) were updated by the original delivery team after these behaviors landed.

## Recorded verification

The final reliable automated run was:

```text
cd agent && bun test extensions/pi-user-agents/tests/{command-line,runner,autocomplete,widget}.test.ts
88 pass, 0 fail, 93 expect() calls
```

Independent checkpoints also recorded:

- live tools GREEN, including exact no-separator acceptance and comma-segment exclusion;
- eager manual paths GREEN for all four option spellings, valid fixtures, recursive fallback, and cursor placement;
- skill/prompt shortcuts GREEN for source filtering, path insertion, spaces, separator handling, filesystem fallback, and extension exclusion.

`git diff --check` reported only the two preserved trailing spaces on the plan status lines. Broad Biome/TypeScript commands still surface known baseline diagnostics; no final task-scoped static ship verdict was recorded by the vanished team.

## Missing final evidence

No durable record proves a real full-stack Pi TUI `/reload` acceptance run for the new tool, manual-path, skill, and prompt-template domains. The last recovery implementer was testing that single gap when its process disappeared. Therefore the honest status is: **implementation and automated behavior are GREEN; final live-reload release proof is missing**.

If work ever resumes, run only that live TUI `/reload` matrix first. If it passes, record the evidence here and perform one final scoped static/doc review. Do not restart implementation or clean the preserved dirty baseline unless that check exposes a real defect.

## Context to preserve

The applicable `AGENTS.md` files, `instruct-another-ai`, TDD/testing, planning, peer-review, and handoff guidance, the full extension/tests, installed Pi editor/autocomplete/runtime sources, and the linked authoritative docs were read. No commit was requested or created.
